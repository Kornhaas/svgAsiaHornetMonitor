camera:
  device: /dev/video0
  width: 1280
  height: 720
  fps: 30
  mjpeg: true

motion:
  # Coordinates are pixels in the configured camera resolution.
  roi:
    x: 160
    y: 90
    width: 960
    height: 540
  min_area: 900
  threshold: 25
  blur_size: 21
  cooldown_seconds: 10

events:
  directory: data/events
  # Save only the motion ROI for a focused, background-free training dataset.
  crop_to_roi: true
  burst_frames: 5
  burst_interval_seconds: 0.25

activity:
  file: data/activity.jsonl

annotations:
  file: data/annotations.jsonl

training:
  # A future training worker may run only inside this overnight window.
  start_hour: 21
  stop_hour: 6
  minimum_annotations: 50

web:
  host: 0.0.0.0
  port: 8000
  debug: false
  auth:
    enabled: false

updates:
  enabled: false
  repository: .
  uv_binary: uv
  service: hornet-monitor.service
